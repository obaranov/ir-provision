[defaults]
name = provisioner
description = Provisions systems using different tools.

[folders] # this section can be removed if default paths are used for plugins folders
settings  = settings
modules   = library
roles     = roles
playbooks = playbooks

[playbooks]
main = provision.yml
cleanup = cleanup.yml
