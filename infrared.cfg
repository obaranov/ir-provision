[defaults]
name = provisioner

[folders] # this section can be removed if default paths are used for plugins folders
settings  = settings
modules   = library
roles     = roles
playbooks = playbooks

[playbooks]
main = provision.yml
cleanup = cleanup.yml
